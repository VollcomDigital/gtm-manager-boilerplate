# Example environment configuration for VD GTM Manager

# Authentication method: user (OAuth desktop client) or service.
GTM_AUTH_METHOD=user

# Absolute path to your OAuth client secrets JSON or service account key.
GTM_CREDENTIALS_PATH=/absolute/path/to/client_secrets.json

# Option A: run exporter for a single salesline.
GTM_SALESLINE=mediamarkt_de

# Option B: supply GTM account/container mappings as JSON.
# Example value (single line, adjust IDs accordingly):
# {"mediamarkt_de":{"account_id":"0000000000","container_id":"GTM-XXXXXXX"}}
GTM_SALESLINES_JSON=

# Optional: fallback direct IDs if not using salesline mapping.
GTM_ACCOUNT_ID=
GTM_CONTAINER_ID=

# Optional: name-based selectors (TypeScript CLI/example supports these, but IDs are preferred)
GTM_ACCOUNT_NAME=
GTM_CONTAINER_NAME=

# Workspace name used for mutations (GTM API v2 edits happen in workspaces)
GTM_WORKSPACE_NAME=Automation-Test

# Example payload used by src/index.ts (GA4 configuration tag)
GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# Export location (inside the repository when running locally).
GTM_OUTPUT_PATH=data/exports/ga4_tags.csv
