# Example environment configuration for GTM Manager Boilerplate

# Authentication method: user (OAuth desktop client) or service.
GTM_AUTH_METHOD=user

# Absolute path to your OAuth client secrets JSON or service account key.
GTM_CREDENTIALS_PATH=/absolute/path/to/client_secrets.json

# Optional: override auth scopes (comma/space-separated).
# Defaults are edit + publish. Add delete scope only if needed.
# GTM_SCOPES=https://www.googleapis.com/auth/tagmanager.edit.containers,https://www.googleapis.com/auth/tagmanager.publish
# GTM_SCOPES=https://www.googleapis.com/auth/tagmanager.edit.containers,https://www.googleapis.com/auth/tagmanager.publish,https://www.googleapis.com/auth/tagmanager.delete.containers

# Option A: run exporter for a single target key from a mapping.
GTM_TARGET_KEY=site_a

# Option B: supply GTM account/container mappings as JSON.
# Example value (single line, adjust IDs accordingly):
# {"site_a":{"account_id":"0000000000","container_id":"GTM-XXXXXXX"}}
GTM_TARGETS_JSON=

# Legacy aliases are still supported:
# GTM_SALESLINE=site_a
# GTM_SALESLINES_JSON=

# Optional: fallback direct IDs if not using target-key mapping.
GTM_ACCOUNT_ID=
GTM_CONTAINER_ID=

# Optional: name-based selectors (TypeScript CLI/example supports these, but IDs are preferred)
GTM_ACCOUNT_NAME=
GTM_CONTAINER_NAME=

# Workspace name used for mutations (GTM API v2 edits happen in workspaces)
GTM_WORKSPACE_NAME=Automation-Test

# Example payload used by src/index.ts (GA4 configuration tag)
GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# Export location (inside the repository when running locally).
GTM_OUTPUT_PATH=data/exports/ga4_tags.csv
