"""Utility helpers for working with GTM triggers."""


class TriggerManager:
    """Placeholder for trigger management operations."""

    def __init__(self, service):
        self.service = service

    def list_triggers(self, account_id: str, container_id: str, workspace_id: str):
        """Return triggers for a workspace (implementation pending)."""
        raise NotImplementedError
