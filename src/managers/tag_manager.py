"""Utility helpers for working with GTM tags."""


class TagManager:
    """Placeholder for tag management operations."""

    def __init__(self, service):
        self.service = service

    def list_tags(self, account_id: str, container_id: str, workspace_id: str):
        """Return tags for a workspace (implementation pending)."""
        raise NotImplementedError
